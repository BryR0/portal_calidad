Connecting to: 192.168.77.53/portalcalidad

--- TABLE: reporte_maestro ---
  itemcod (VARCHAR(64) COLLATE "utf8mb4_unicode_ci")
  itemcodbarra (VARCHAR(64) COLLATE "utf8mb4_unicode_ci")
  itemnombre (VARCHAR(255) COLLATE "utf8mb4_unicode_ci")
  marcanomb (VARCHAR(255) COLLATE "utf8mb4_unicode_ci")
  areanombre (VARCHAR(255) COLLATE "utf8mb4_unicode_ci")
  departamentos (VARCHAR(255) COLLATE "utf8mb4_unicode_ci")
  categorianom (VARCHAR(255) COLLATE "utf8mb4_unicode_ci")
  grupo (VARCHAR(255) COLLATE "utf8mb4_unicode_ci")
  rechazado (TINYINT)
  fechacreacion (DATETIME)
  fechaultcompra (DATETIME)
  costo (DECIMAL(18, 4))
  precio (DECIMAL(18, 4))
  idmarca (INTEGER)
  iddepartamento (INTEGER)
  idcategoria (INTEGER)
  idgrupo (INTEGER)
  idarea (INTEGER)
  nomsuplidor (VARCHAR(255) COLLATE "utf8mb4_unicode_ci")
  extranjero (TINYINT)
  idsuplidor (VARCHAR(64) COLLATE "utf8mb4_unicode_ci")
  empaque (VARCHAR(128) COLLATE "utf8mb4_unicode_ci")
  razonrechazado (VARCHAR(255) COLLATE "utf8mb4_unicode_ci")
  noseusa (TINYINT)
  unidaduso (VARCHAR(64) COLLATE "utf8mb4_unicode_ci")
  unidadalmacen (VARCHAR(64) COLLATE "utf8mb4_unicode_ci")
  unidadcompra (VARCHAR(64) COLLATE "utf8mb4_unicode_ci")
  pack (DECIMAL(18, 4))
  idgrupo09 (INTEGER)
  fechacambio (DATETIME)

--- TABLE: rsi_cliente_suplidor ---
  cliente_suplidor (VARCHAR(64) COLLATE "utf8mb4_unicode_ci")
  nombre (VARCHAR(255) COLLATE "utf8mb4_unicode_ci")
  extranjero (TINYINT)
  ruc (VARCHAR(128) COLLATE "utf8mb4_unicode_ci")
  idtercero (VARCHAR(64) COLLATE "utf8mb4_unicode_ci")
  activo (TINYINT)
